name: PUISSANCE-MD 
services:
  - name: PUISSANCE-MD 
    type: web
    ports:
      - 5000
    routes:
      - path: "/"
    build:
      type: docker
      dockerfile: ./Dockerfile
    regions:
      - fra
      - sin


      

